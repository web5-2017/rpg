<ul class="nav nav-tabs">
  <%= li_active(match_game_path(@game))  do %>
    <% link_to 'Histórias', match_game_path(@game) %>
  <% end %>
  <%= li_active(match_game_items_path(@game))  do %>
    <% link_to 'Items', match_game_items_path(@game) %>
  <% end %>
</ul>
