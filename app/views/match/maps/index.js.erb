$('#tab-content').html("<%= escape_javascript(render partial: 'index', locals: {  } ) %>");
$('.nav-tabs li').removeClass('active');
$('#maps').addClass('active');
location.hash = $('#maps').attr("id");
