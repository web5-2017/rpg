<tr>
  <td><%= item.name %></td>
  <td>
    <%= link_to 'Editar', edit_match_game_item_path(@game, item), class: 'btn btn-primary' %>
    <%= link_to 'Deletar', match_game_item_path(@game, item),
                           class: 'btn btn-danger',
                           method: :delete%>
  </td>
</tr>
