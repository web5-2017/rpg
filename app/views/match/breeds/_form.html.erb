<%= simple_form_for([:match, @history, @breed]) do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>

  <%= f.button :submit, class: "btn btn-primary" %>
  <%= link_to "Voltar", :back, class: "btn btn-default" %>
<% end %>
