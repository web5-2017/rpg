<% provide(:header, "Partida #{@game.name} - Novo Desafio") %>
<% provide(:title, "Partida #{@game.name}|Desafios") %>

<div class="panel panel-default">
  <div class="panel-body">
    <%= render 'form' %>
  </div>
</div>
