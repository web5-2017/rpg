<ul id="sidebar" class="nav nav-pills nav-stacked">
  <%= li_active match_root_path(@game) do %>
    <% link_to 'Inicio', match_root_path(@game) %>
  <% end %>

  <%= li_active match_character_path(@game) do %>
    <% link_to 'Personagem', match_character_path(@game) %>
  <% end %>
</ul>
