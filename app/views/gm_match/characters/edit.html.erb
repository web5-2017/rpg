<div class="page-header text-center">
  <h2>Editando <%= @character.name %></h2>
</div>

<%= render partial: 'form', locals: { url: gm_match_character_path(@game.id, @type, @character),
                                      submit: "Atualizar #{@type}"} %>
