<% provide(:header, "Partida #{@game.name} - Editar Item #{@item.name}") %>
<% provide(:title, "Partida #{@game.name}|Items") %>

<div class="panel panel-default">
  <div class="panel-body">
    <%= render 'form' %>
  </div>
</div>
